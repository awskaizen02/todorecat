version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install      
  build:
    commands:
      - npm run build
      - npm install -g serve

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build
